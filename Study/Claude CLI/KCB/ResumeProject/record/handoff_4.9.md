# handoff_4.9.md
> 작성일: 2026-04-09
> 이어받는 세션(Claude Code)이 읽을 인수인계 노트

---

## 4.5 이후 변경사항

### 1. 채빈 파싱 완료
- `users/채빈/raw/` 에 자소서 파일들을 넣고 파싱 실행
- HWP/HWPX/DOCX/PDF 변환 처리 포함
- 생성된 파일:
  - `users/채빈/writing-style.md`
  - `users/채빈/profile.md` (GPA: 4.05/4.5 미확정)
  - `users/채빈/index.md`
  - `users/채빈/경험/` 18개 파일

### 2. 자소서 작성 테스트 완료
- BGF리테일 영업관리 직무, 500자 (띄어쓰기 포함) → 497자 작성
- `outputs/채빈/BGF리테일_영업관리.md` 저장됨

### 3. 아이디어 파일 구현사항 반영

#### parse-experiences.md 변경
- **Step 0 추가**: HWP/HWPX/DOCX/PDF 파일 자동 변환 로직
  - HWPX: ZIP 열어 `Contents/section0.xml` 파싱 (`<hp:t>` 태그)
  - HWP (구버전): `/Users/chaebin/Library/Python/3.9/bin/hwp5txt` 사용
  - DOCX: ZIP 열어 `word/document.xml` 파싱 (`<w:t>` 태그)
  - PDF: Claude 직접 읽기
  - 100자 미만이면 스킵
- **Step 1 변경**: raw/ 루트가 아니라 `apply/`, `logs/` 서브폴더 구조 사용 (아래 참고)
- **Step 2 변경**: writing-style.md는 `apply/`만 참고, 최근 파일 가중치 높게
- **Step 3 변경**: 경험 추출은 `apply/` + `logs/` 모두 참고
- **Step 5 추가**: 관심직무.md 처리 — 파일 없으면 DB에서 추론 후 확인받아 생성, 키워드는 DB 실존 키워드만 사용
- **관심직무.md 키워드 작성 원칙 추가**: 사용자 입력 + DB(index.md 키워드별 항목)에 실존하는 것만 사용. 배경지식으로 임의 추가 금지

#### write-coverletter.md 변경
- 글자수 기본값: **띄어쓰기 포함**
- 직무 파악: `관심직무.md`에서 해당 직무 키워드 참조 (직무 미입력 시 목록 보여주고 선택)
- 활동 수: **최대 2개, 기본 1개**, 깊이 있게 작성 (3개 이상 나열 금지)
- output 저장: `outputs/[이름]/[기업명]_[직무명].md`, 같은 파일 있으면 append

#### query-experiences.md 변경
- 이력 5개 카테고리로 세분화: 실무경험 / 수상경력 / 학술·연구 / 대외활동·봉사 / 교육·자기계발
- **직무 기반 관련도 조회** 추가: `[이름] [직무명] 직무로 이력 보여줘` 트리거
  - `관심직무.md`에서 해당 직무 키워드 읽어 관련도 순 정렬

### 4. 관심직무.md 도입
- 직무 관련도를 experience 파일의 정적 필드가 아닌 사용자별 `관심직무.md`로 관리
- 채빈의 현재 관심직무: 물류/SCM 운영기획, 포워딩 (2개)
- 키워드: DB에 실존하는 것만 (물류/SCM, 데이터분석, 글로벌/해외경험, 기획/아이디어, 커뮤니케이션)

### 5. raw/ 서브폴더 구조 도입
```
raw/
  apply/   ← 자소서, 지원서, 신청서, 포트폴리오 (어투 분석 + 경험 추출)
  logs/    ← 소감문, 활동일지, 결과보고서 (경험 내용 보강용)
```
- 채빈 파일 분류 완료: apply 14개, logs 5개
- 파싱 시 apply/logs/ 없으면 자동 분류 후 사용자 확인 받는 로직 추가

### 6. 아이디어 파일 정리
- 완료 / 예정으로 구분해 정리

---

## 현재 채빈 파일 구조
```
users/채빈/
  raw/
    apply/  ← 14개 (자소서, 지원서, 포트폴리오 등)
    logs/   ← 5개 (소감문, 결과보고서, 주간활동서 등)
  experiences/  ← 18개 파일
  writing-style.md
  profile.md
  index.md
  관심직무.md
outputs/채빈/
  BGF리테일_영업관리.md
```

---

## 미결 사항

| 항목 | 내용 |
|------|------|
| 옛날 경험 처리 | 너무 오래된 경험은 스루하기 전에 먼저 사용자에게 물어보는 로직 |
| 기업별 톤앤매너 | 기업 정보/분위기를 반영한 자소서 작성 |
| 면접 질문 생성 | CEO 발표 기사/키워드 스크랩 연동 |
| 지원자+기업 정보 연동 | 맞춤형 작성 강화 |
| README / 새 사용자 온보딩 | 빈 폴더 구조 템플릿 |

---

## 결정 사항 및 배경

| 결정 | 이유 |
|------|------|
| raw/ 서브폴더 apply/logs/ 분리 | 어투 분석은 지원서만, 경험 내용은 소감문/활동일지도 포함해야 더 풍부 |
| 관심직무.md 도입 | 직무 관련도를 experience 파일 정적 필드로 관리하면 유연성 없음, 사용자별로 직무가 다름 |
| 관심직무.md 키워드 = DB 실존 키워드만 | 배경지식으로 키워드를 추가하면 DB와 맞지 않는 매칭이 발생함 |
| output: 기업명_직무명.md | 같은 직무에 여러 문항이 쌓이도록 append 구조 |
